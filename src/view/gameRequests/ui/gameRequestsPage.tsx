import React from "react";

export interface GameRequestsPageProps {}

const GameRequestsPage: React.FC<GameRequestsPageProps> = ({}) => {
  return <span>Game requests page</span>;
};

export default React.memo(GameRequestsPage);
