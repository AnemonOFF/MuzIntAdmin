import React from "react";

export interface GamePacksPageProps {}

const GamePacksPage: React.FC<GamePacksPageProps> = ({}) => {
  return <span>Game packs page</span>;
};

export default React.memo(GamePacksPage);
